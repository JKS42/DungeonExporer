# Prompt evaluation set (TODO)

Placeholder for a fixed set of ~20 prompts used to compare models (`qwen3:4b` vs `llama3`) per [`ollama-plan.md`](../ollama-plan.md) §1.

Suggested format: `prompts.jsonl` with fields `id`, `category` (npc|flavor), `prompt`, `expected_traits` (max_words, tone).

Not required for minimum submission; add before tuning model choice.
